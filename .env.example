# バックエンドAPIのベースURL（/api は付けない。api.js が自動で付与する）
# 開発時は未設定でOK（http://localhost:3000 にフォールバック）。
# 本番（Render）では Static Site の環境変数にデプロイ済みAPIのURLを設定する。
#   例: VITE_API_BASE_URL=https://yo-backend.onrender.com
VITE_API_BASE_URL=http://localhost:3000
